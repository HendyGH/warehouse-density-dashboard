const assert = require('assert'); const fs = require('fs'); const vm = require('vm'); const generic = JSON.parse(fs.readFileSync('src/profiles/generic.json', 'utf8')); const context = { console, window: {}, fetch: async () => ({ ok: true, json: async () => generic }) }; vm.runInNewContext(fs.readFileSync('src/profile.js', 'utf8'), context); (async () => { const api = await context.window.WarehouseProfileReady; assert.strictEqual(api.profile.id, 'generic'); assert.deepStrictEqual(api.getCategoryIds(), ['general']); assert.strictEqual(api.getSpecialLocations().length, 0); console.log('generic warehouse fixture passed'); })().catch(error => { console.error(error); process.exitCode = 1; });

