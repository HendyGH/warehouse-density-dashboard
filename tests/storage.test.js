const assert = require('assert'); const fs = require('fs'); const vm = require('vm'); const context = { window: { localStorage: { getItem: () => null, setItem: () => {}, removeItem: () => {} } } }; vm.runInNewContext(fs.readFileSync('src/app/storage.js', 'utf8'), context); assert.strictEqual(context.window.SharedStore.get('missing', 'fallback'), 'fallback'); context.window.MachineConfig.set('activeProfilePath', './profiles/generic.json'); assert.strictEqual(context.window.MachineConfig.get('activeProfilePath'), './profiles/generic.json'); console.log('storage tests passed');

